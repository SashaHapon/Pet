spring:
  application:
    name: consumer
  profiles:
    active: consumer
  cloud:
    config:
      uri: http://cloud-config:8888
      fail-fast: true
      enabled: true
      retry:
        max-attempts: 20
  datasource:
    driver-class-name:
    url: jdbc:postgresql://localhost:5432/testdb
    username: postgres
    password: postgres